{
  "name": "elora-ltd",
  "version": "1.0.0",
  "description": "Elora Services Limited",
  "author": "dwighson",
  "private": true,
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "nuxt generate",
    "build:gh-pages": "DEPLOY_ENV=GH_PAGES nuxt build",
    "generate:gh-pages": "DEPLOY_ENV=GH_PAGES nuxt generate",
    "deploy": "push-dir --dir=dist --branch=G=gh-pages --cleanup"
  },
  "dependencies": {
    "cross-env": "^5.2.0",
    "jquery": "^3.3.1",
    "nuxt": "^2.0.0",
    "slick-carousel": "^1.8.1"
  },
  "devDependencies": {
    "nodemon": "^1.11.0",
    "push-dir": "^0.4.1"
  }
}
