{
  "name": "elora-ltd",
  "version": "1.0.0",
  "description": "Elora Services Limited",
  "author": "dwighson",
  "private": true,
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "nuxt generate",
    "build:gh-pages": "DEPLOY_ENV=GH_PAGES nuxt build",
    "generate:gh-pages": "DEPLOY_ENV=GH_PAGES nuxt generate",
    "deploy": "push-dir --dir=dist --branch=gh-pages --cleanup"
  },
  "dependencies": {
    "cross-env": "^5.2.0",
    "emailjs-com": "^2.3.2",
    "jquery": "^3.3.1",
    "nuxt": "^2.0.0",
    "nuxt-cname-module": "^1.1.1",
    "slick-carousel": "^1.8.1",
    "storyblok-nuxt": "^1.0.1"
  },
  "devDependencies": {
    "nodemon": "^1.11.0",
    "push-dir": "^0.4.1"
  }
}
